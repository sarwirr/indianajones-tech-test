export type TrajetInformation = {
    
    depart: string;
    destination: string;
    heureDepart: string;
    duree: string;
};

export type TrajetInput = {
    depart: string;
    destination: string;
    heureDepart: string;
};